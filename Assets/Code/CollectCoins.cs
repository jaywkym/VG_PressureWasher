using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace PressureWasher{
public class CollectCoins : MonoBehaviour
{

    //NOTE: technically this file is unneccessary, since the code has been added to player.cs
    //however, this file might be useful in the future if we were to take this approach instead of adding the script to player.cs


    //// Start is called before the first frame update
    //void Start()
    //{

    //}

    //// Update is called once per frame
    //void Update()
    //{

    //}
}
}

